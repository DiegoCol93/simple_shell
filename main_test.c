/**
 * Main from print_list
 *   int main(void)
 *   {
 *	   char *name = "PATH";
 *	   list_t *head;
 *
 *	   head = list_path(name);
 *	   free_list(head);
 *	   head = NULL;
 *	   return (0);
 *   }
 */

/**
 * Main from print_path
 * main - prints the environment using environ.
 *
 * Return: Always 0.
 * /end of comment.
 *  int main(void)
 *  {
 *        print_path("PATH");
 *        return (0);
 *  }
 */
